<!DOCTYPE html>
<html>
  <%- include('partials/head'); -%> <%- include('partials/navbar'); -%>
  <body class="bg-color">
    <div class="container">
      <div
        class="col-12 my-5 d-flex align-items-center justify-content-center flex-column"
      >
        <div class="bg-white p-3 rounded">
          <h2 class="text-dark text-center sans-bold">Edit Your Expense!</h1>
        <br />
          <form
            action="/dashboard/<%= expenseInfo.group_id %>/<%= expenseInfo.id %>/edit?_method=PUT"
            method="POST"
          >
            <label for="name">Name :</label>
            <input
              type="text"
              id="name"
              name="name"
              size="50"
              value="<%= expenseInfo.name %>"
              class="form-control text-dark"
              required
            /><br />
            <label for="date">Date of Expense:</label>
            <input type="date" id="date" name="date" size="50"
            value="<%=moment(expenseInfo.date).format("YYYY-MM-DD")%>"
            class="form-control text-dark" required /><br />
            <label for="note">Comments:</label><br />
            <input
              type="text"
              id="note"
              name="note"
              size="50"
              value="<%= expenseInfo.note  %>"
              class="form-control text-dark"
            /><br />
            <label for="amount">Enter Amount</label>
            <input
              type="text"
              name="amount"
              id="amount"
              size="50"
              class="form-control text-dark"
              pattern="^\d{1,3}(,\d{3})*(\.\d+)?$"
              value="<%= expenseInfo.amount %>"
              data-type="currency"
              placeholder="$1000.00"
            />
            <br />
            <input
              type="submit"
              value="Submit"
              class="btn btn-primary text-center btn-lg rounded-0"
            />
          </form>
          <br/>          
  
        <!-- <form
            action="/dashboard/<%= expenseInfo.group_id %>/<%= expenseInfo.id %>/delete?_method=DELETE"
            method="POST">
              <input
                class="btn btn-danger btn-lg rounded-0 btn-block"
                type="submit"
                value="Delete"
              />
          </form> -->
          <button data-toggle="modal" data-target="#deleteExp<%= expenseInfo.id %>" 
          class="btn btn-danger btn-lg btn-block rounded-0">Delete</button>
  <!-- Modal -->
          <div class="modal fade" id="deleteExp<%= expenseInfo.id %>" tabindex="-1" aria-labelledby="deleteGroupLabel" aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered" role="document">
          <div class="modal-content">
          <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Warning</h5>
          
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Do You Really want to delete this Expense?
      </div>
      <div class="modal-footer d-inline-flex flex-row align-items-start">
        <button type="button" class="btn btn-secondary px-4 rounded-0" data-dismiss="modal">Close</button>
        <form id="deletegroup" action="/dashboard/<%= expenseInfo.group_id %>/<%= expenseInfo.id %>/delete?_method=DELETE" method="POST">
          <label>
            <input class="btn btn-danger rounded-0 px-3" type="submit" value="Delete" />
          </label>
      </div>
    </div>
  </div>
</div>
<!-- End of Modal -->      
    </div>
  </div>
 
  <%- include('partials/dashboard-footer'); -%> 
  </body>
</html>
